# Execution notes — deviations and carry-forwards from review findings

Controller-maintained log of approved deviations from the plan text discovered during execution. Every task dispatch must consult this file; where it conflicts with the plan, THIS FILE WINS.

## Approved deviations from plan text

0. **(Task 14, vendor-verified)** The publish permission gates publish-state transitions in EITHER direction (v6 CP authorizes 'publish' for unpublish too — PublishedEntriesController store+destroy); same-state sends are free. Spec §6 amended accordingly. Terms/globals have no publish state, so this applies only to entry tools (T16 revisions must respect it too).

1. **(Task 3, from Pest 4.7.4 reality)** `DisabledMcpTestCase` class is impossible (Pest forbids two class bindings per file). Shipped as trait `tests/DisablesMcp.php` instead. Same pattern applies to any future per-file test-case override: use a trait (e.g. `tests/UsesOAuthMode.php`).
2. **(Task 3 quality review, CRITICAL)** ServiceProvider builds + validates the full middleware stack BEFORE calling `Mcp::web()` (fail-closed: a config-shape throw must leave zero routes registered, never an unauthenticated route), and `Arr::wrap()`s the configured middleware.
3. **(Task 3 quality review, IMPORTANT)** OAuth branch uses a single wrapper middleware `Danielgnh\StatamicMcp\Middleware\AuthenticateOAuth::class` — NOT the plan's `[EnsureOAuthConfigured::class, 'auth:api']` pair. Reason: Laravel's middleware priority hoists `auth:api` (AuthenticatesRequests) above the preflight at runtime, 500ing on missing api guard and disabling pre-auth throttle.
4. **(Task 3 quality review)** Middleware-order tests must assert the RESOLVED pipeline (`app('router')->gatherRouteMiddleware($route)`), never the declared array.

## Carry-forward instructions for future tasks

- **Tasks 10+ (all tools):** tests/TestCase.php registers `Laravel\Mcp\Server\McpServiceProvider` via getPackageProviders() (T9 fix) — AddonTestCase skips composer discovery, and without it every parameterized tool call in tests sees EMPTY arguments ("The type field is required."). Production is unaffected (auto-discovery). Do not remove; if a tool test sees empty args, check this first. Also: v6 auto-injects a `slug` field into entry/term blueprints and duplicates `required` on injected title (dedupe with array_unique) — expect these in Fields-API views.
- **Tasks 10+ (all tools):** base Tool now provides throwing guards `ensureWritesEnabled()`/`ensureDeletesEnabled()` (canonical denial messages naming the config switch) and boolean `can(UserContract, string)` — USE THESE, never hand-roll read-only/deletes-disabled/permission-flag logic (T8 quality review).
- **Tasks 16/20/21 (delete/revision paths):** v6's Entry::delete() THROWS ('Cannot delete an entry with localizations.') on origins with descendants — never call it bare; T15's pattern: ensureSiteAccess on EVERY descendant locale first, deleteDescendants(), survivor re-check (vendor silently ignores per-descendant EntryDeleting cancellations), then origin delete() return check; response enumerates deleted_localizations. Check whether Term/GlobalVariables deletes have analogous cascade/throw semantics before writing T20/21/22 code.
- **Globals facts (T22):** Variables has NO TracksLastModified (no managed metadata — do not strip updated_at from globals data; it would corrupt legit fields); Variables::save() returns false on cancelled Creating/Saving; GlobalVariablesStore strips an `origin` data key on rehydration (reserved-key rejection enforced even blueprint-less via extracted rejectReservedKeys()); GlobalSet::localizations() is Blink-cached — persisted-state assertions after cancelled saves need Blink::flush() IN ADDITION to Stache::clear().
- **Tasks 21–22 (from T20):** shared concern `ComparesPatchData` now owns normalize()+strict-compare and rejectPreviewObjects() (EntriesUpdate + TermsUpdate use it) — globals_update MUST reuse it. Term renames: syncOriginal() on ->term() before mutating (file-hydrated terms have no synced original; without it renames orphan files and never rewrite references); Term::find returns LocalizedTerm. Test-env: Stache array cache stores LIVE references — rebuild via Stache::clear() before asserting persisted state; never single-arg tap().
- **Tasks 20–21 (term writes, from T19):** term slugs normalize via plain `Statamic\Support\Str::slug()` — language-AGNOSTIC (vendor Term::slug() setter; unlike entries' site-lang Routable) — matching it keeps collision checks aligned with persisted ids. v6 injects a required `slug` field into term blueprints: resolve the slug BEFORE blueprint validation and feed it in as data. Terms need NO withReplacements (CP passes none; the id IS the uniqueness key). updateLastModified() lives on LocalizedTerm, not Term.
- **Tasks 18–22 (term/global tools):** terms use `value()` for BOTH title and updated_at (locale fallback coherence — T17 decision; terms_get must mirror). Term ids are `taxonomy::slug`, stable across localizations — the `site` param is the separate axis. ResolvesSites now distinguishes "not available for this resource" (validSites rejection incl. defaulted-site nuance) from "not found" (global nonexistence) — reuse, don't re-word.
- **Tasks 17+ (all list tools):** copy entries_list's PINNED ordering pattern (T10 fix): deterministic orderBy + id tiebreaker, ordering stated in the #[Description]; validate site/search as nullable|string (laravel/mcp doesn't enforce JSON schema server-side); positive multi-site assertions (create content in BOTH sites, assert filtering) — never zero-content site tests. Use $model->value('title') for origin-chain fallbacks.
- **Tasks 14/19/20/22 (all write tools):** validateAgainstBlueprint(Blueprint, array $merged, array $replacements = []) — pass CP-parity replacements: creates ['collection'=>…,'site'=>…]; updates additionally 'id' => $entry->id() (unique_entry_value scoping). rejectUnknownKeys hard-rejects reserved handles (id, origin, published, blueprint) before blueprint lookup. Slug handling: normalize with Statamic's Str::slug($slug,'-',Site::get($site)->lang()) BEFORE any collision query; reject empty-after-normalization. ALWAYS check save() return value (listeners can cancel — report failure, never false success). updateLastModified($user) before save for CP parity.
- **Task 14 (entries_update) — REQUIRED (from T13):** the ValidatesBlueprintData concern excludes `slug` from valid handles; the CALLER owns the dated-collection `data.date` rejection ("pass date as a top-level parameter, not inside data") — replicate that guard; resolve date BEFORE blueprint validation and feed the Carbon into the validator (v6 injects a required `date` blueprint field on dated collections; the DateFieldtype rule accepts Carbon, rejects plain strings).
- **Task 14 (entries_update) — REQUIRED (from T11 quality review):** (a) reject any incoming data value shaped like a preview object ({__preview, truncated, note}) with an error telling the agent to fetch raw first — the one raw-path artifact an agent could accidentally write back; (b) ignore/strip updated_at & updated_by if present in the patch (entries_get already strips them from raw output, but stale copies may exist in agent context).
- **Tasks 12/14/15 (entry tools):** use the reshaped ResolvesEntries::findExposedEntry(id, user, ?site) — it does exposure + site-match + ensureSiteAccess in ONE place (T11 critical fix); never fetch entries by bare id.
- **Task 10/12 + 17 (site handling):** revisit whether statamic_overview's `sites` block should gain a per-site access flag in multisite ('access {site} site' only registers when multisite enabled) — the model shouldn't be offered sites it will be denied on (T8 quality review #5).

- **Task 6 (AuthenticateMcpToken):** the class must NEVER implement `Illuminate\Contracts\Auth\Middleware\AuthenticatesRequests` — Laravel's priority sorter would hoist it above the configured throttle. The Task 3 resolved-pipeline test pins this; leave a comment on the class.
- **Task 6 (optional polish, from T4 quality review):** add one test to `tests/Feature/EnsureMcpPermissionTest.php` passing a generic `Authenticatable` that is NOT a Statamic UserContract → expect 403 with the generic message (locks in the Stache UserRepository's fail-closed `fromUser` semantics).
- **Task 23 (OAuth mode):** implement `src/Middleware/AuthenticateOAuth.php` (single class: oauth preflight → 503-with-remedy, then delegate to the `api` guard, e.g. via `auth()->shouldUse('api')` + manual Authenticate invocation or guard check) INSTEAD of the plan's separate `EnsureOAuthConfigured` + `'auth:api'`. Must not implement `AuthenticatesRequests`. Adapt the plan's T23 tests: assert resolved pipeline has `AuthenticateOAuth` before `EnsureMcpPermission` and does NOT contain `auth:api`/`Authenticate`. The provider already forward-references this class (Task 3 fix commit).
- **Task 24/26:** mcp:doctor + README should mention the "enabled but failed to mount" state (ServiceProvider logs `Statamic MCP failed to mount; run php please mcp:doctor` via Log::warning on caught boot failure).
- **Task 24 (from T23 review):** doctor must mirror the FULL oauth preflight incl. driver checks: api guard exists AND driver === 'passport'; users repository check should verify the resolved DRIVER (config('statamic.users.repositories.'.repository.'.driver')), not the repository name; check HasApiTokens trait on the provider's model when Passport present.
- **Task 27/28 (Passport CI leg — from T23 review, 9 items):** future passport-installed integration suite must cover: (1) all-green HTTP path (real Bearer → 200 through the full pipeline); (2) token lifecycle 401s (expired/revoked/malformed/wrong-audience — never 500); (3) HasApiTokens-missing → pinned behavior; (4) WWW-Authenticate rewrite with resource_metadata + RFC 9728 metadata dereference (authorization_servers, scopes_supported: ['mcp:use']); (5) oauthRoutes endpoints + DCR; (6) SCOPE ENFORCEMENT DECISION: Passport's guard does NOT enforce mcp:use — confirm 'access mcp' permission is the intended sole gate or add a scope check, then pin; (7) real Eloquent normalization (fromUser wraps the Passport model, roles from DB); (8) wrong-driver 503; (9) statelessness (no session cookie on MCP responses).
- **Task 24 (from T7 quality review):** in the full Doctor: (a) when tokens.yaml already exists, ALSO check is_writable on the file itself (root-issued/deploy-user ownership mismatch passes the ancestor-dir probe but fails writes); (b) add a test covering Doctor's [FAIL] branch + FAILURE exit (chmod 0500 on the probe ancestor, restored in finally); (c) consider APP_URL-default and plain-http warnings (Bearer over http is the adjacent trap).
- **Task 27 (CI):** add a `<source><include><directory>src</directory></include></source>` block to phpunit.xml (T1 quality suggestion — deferred here); PHPStan as planned (also catches the provider's forward class references).
- **Task 28 (release):** add composer.json `keywords` (statamic, mcp, addon, ai) before Packagist submit (T1 quality suggestion).
- **Task 16 (revisions):** deleting an entry orphans its revision/working-copy files on disk — CP-parity behavior, DOCUMENT it (tool description or README), don't fix. The publish-transition rule (deviation 0) applies to revision publish paths too.
- **Pre-release polish sweep (+2 from T22 re-review):** (a) ComparesPatchData's preview-guard remediation text fabricates a `fields` param for globals_get — make the fields-hint conditional or use a globals-specific message; (b) globals omit-handle path returns every set's full data uncapped — if real payloads bite, TermsGet's preview machinery is directly reusable (candidate, not defect).
- **Pre-release polish sweep (+1 from T21 re-review):** add a multi-term array-shape reference-strip test to TermsDeleteTest (entry topics ['php','laravel'] → ['laravel'] survives re-indexed) — pins vendor replaceValuesInArray behavior the string-shape test can't catch.
- **Pre-release polish sweep (+1 from T16 re-review):** entries_update's `data` rule is required|array, and Laravel's `required` fails on [] — a slug-only update must send a non-empty data object. Consider `present|array` (or slug-only calls) as a UX papercut fix.
- **Pre-release polish sweep (+2 from T15 re-review):** report($cascadeFailure) before the survivor ToolException in EntriesDelete (observability, zero behavior change); pin the "N localizations were already deleted" message branch with a bushy-tree test when a fourth fixture site becomes convenient.
- **Pre-release polish sweep (optional minors from T6 quality review, batch before v1.0.0):** (a) dataset-driven test pinning identical 401 status+header+body across ALL auth failure classes (anti-enumeration regression pin); (b) one positive-path test for case-variant `bearer` scheme (inherited from framework's strripos); (c) `$record['hash'] ?? ''` / `expires_at` null-coalesce in AuthenticateMcpToken to degrade hand-edited-record errors from 500 to clean 401; (d) optional dummy-hash compare when record missing (timing uniformity; not exploitable at current entropy).
- **All tasks:** never commit `composer-integrity.lock` (machine-local soak-time plugin artifact, gitignored since 81bf2f8). Never run `pest --parallel` (shared dev-null sandbox). Never run the bare `vendor/bin/testbench` CLI — it writes a `.env` + bootstrap cache into the shared skeleton and breaks the suite ("no such table: cache"); verify command registration through the test harness's console Kernel instead (T7 incident).
